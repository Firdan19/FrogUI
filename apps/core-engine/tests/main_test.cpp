// Catch2 provides its own main when linked with Catch2WithMain
// However, we just define an empty placeholder to ensure compilation if needed,
// but Catch2::Catch2WithMain handles it. We can just leave it empty.
